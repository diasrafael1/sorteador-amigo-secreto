import styled from "styled-components";

export const FriendSpan = styled.p`
  font-size: 1.8rem;
  line-height: 2.7rem;
  color: rgba(254, 101, 43, 0.99);
  mix-blend-mode: normal;
  margin: 1.4rem 0 2rem;
`;
